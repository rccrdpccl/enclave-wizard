---
# OSAC plugin defaults — enclave-wizard override
# Pins images to versions compatible with chart 0.0.1 (no ExternalIPPool CRD).
# This file replaces plugins/osac/defaults.yaml in the enclave RPM.
osacProfilesList:
  - caas
  - vmaas

# AAP settings (chart manages the instance, these configure it)
osac_aap_name: osac-aap
osac_aap_operator_ns: ansible-aap
osac_aap_api_user: admin
osac_aap_validate_certs: false

# Keycloak integration
osac_keycloak_ns: keycloak
osac_keycloak_name: keycloak
osac_keycloak_svc_name: keycloak-service
osac_keycloak_realm: osac
osac_keycloak_client_id: osac-controller
osac_keycloak_service_port: 8443

# Fulfillment database (built-in dev postgres)
osacBYODatabase: false
osac_db_name: postgres
osac_db_image: "quay.io/sclorg/postgresql-15-c9s:latest"
osac_db_size: 5Gi
osac_db_user: service
osac_db_database: service

# AAP bootstrap
osac_aap_project_git_uri: "https://github.com/osac-project/osac-aap"
osac_aap_project_git_branch: "3453b544"

# Container images — pinned to chart 0.0.1 (published 2026-06-11)
# All images from the same day as the chart publication.
osac_images:
  operator: "ghcr.io/osac-project/osac-operator"
  operator_tag: "sha-0c69537"
  fulfillment_service: "ghcr.io/osac-project/fulfillment-service:v0.0.64"
  envoy: "docker.io/envoyproxy/envoy:v1.33.0"
  aap_bootstrap: "ghcr.io/osac-project/osac-aap:sha-3453b54"
  cli: "quay.io/openshift/origin-cli:4.20.0"
