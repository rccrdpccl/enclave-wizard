{
  "name": "@enclave-wizard-ui/ioc",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "clean": "rm -rf node_modules dist .tmp",
    "check": "yarn run -T biome check .",
    "check:fix": "yarn run -T biome check --write ."
  },
  "peerDependencies": {
    "react": "^18.0.0"
  }
}
